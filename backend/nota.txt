export const agregarContacto = async (nuevoContacto) => {
  try {
    await axios.post(URL_API, nuevoContacto);
    await cargarContactos();  // Recargar la lista desde la API
  } catch (error) {
    console.error("Error al agregar el contacto:", error);
  }
};


  function resetForm() {
    nombre = "";
    sexo = "Masculino";
    profesion = "";
    edad = 18;
    habla_ingles = false;
  }